 # Kotlin Multiplatform Todo App (Nav3)
A modern Kotlin Multiplatform (KMP) project demonstrating the new Jetpack Navigation 3 architecture for shared UI across Android and iOS. This project uses a state-driven navigation model where the backstack is a simple list of type-safe keys.
